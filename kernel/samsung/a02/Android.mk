LOCAL_PATH := $(call my-dir)

# This placeholder tree intentionally contributes no modules.
# The real kernel sources should be provided by a proper kernel repository.
# Keeping the file empty avoids bogus module definitions during the build.

